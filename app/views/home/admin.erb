<div class="container">
  <div class="row">
    <div class="col s12">
      <ul class="tabs">
        <li class="tab col s3"><a href="#test1">입금확인전</a></li>
        <li class="tab col s3"><a href="#test2">입금확인후</a></li>
        <li class="tab col s3"><a href="#test3">기간만료</a></li>
        <li class="tab col s3"><a href="#test4">회원관리</a></li>
      </ul>
    </div>
    <div id="test1" class="col s12">
      <table class="responsive-table highlight">
        <thead>무통장입금정보
          <tr>
              <th>No.</th>
              <th>신청날짜</th>
              <th>신청자(입금자)</th>
              <th>상품명</th>
              <th>금액</th>
              <th>받는사람</th>
              <th>받는사람번호</th>
              <th>아이디</th>
              <th>핸폰번호</th>
              <th>확인</th>
          </tr>
        </thead>
        <tbody>
            <% @contact.each do |c| %>
              <tr>
              <% unless c.post.menu_id == nil or c.post.menu_id == '' %>
                <% if c.title == nil && c.created_at >= Date.today-2  %>
                    <td><%=c.id%></td>
                    <td><%=c.created_at%></td>
                    <td><%=c.writer_email%></td>
                    <td><%=c.post.menu.menu_name%></td>
                    <td><%=c.post.menu.price%></td>
                    <td><%=c.post.receiver_name%></td>
                    <td><%=c.post.receiver_number%></td>
                    <td><%=c.user.email%></td>
                    <td><%=c.tel_number%></td>
                    <td>
                        <form action="/home/check_pay/<%=c.id%>" method="get">
                          <input type="hidden" value="1" name="title"/>
                          <button class="btn waves-effect waves-light" type="submit" name="action" onclick="return confirm('입금확인하셨습니까?')" >확인</button>
                        </form>
                    </td>
                <% end %>
              <% end %>
              </tr>
            <% end %>
        </tbody>
      </table>
    </div>
    <div id="test2" class="col s12">
      <table class="responsive-table highlight">
        <thead>무통장입금정보
          <tr>
              <th>No.</th>
              <th>신청날짜</th>
              <th>신청자(입금자)</th>
              <th>상품명</th>
              <th>금액</th>
              <th>받는사람</th>
              <th>받는사람번호</th>
              <th>아이디</th>
              <th>핸폰번호</th>
              <th>확인</th>
          </tr>
        </thead>
        <tbody>
            <% @contact.each do |c| %>
              <tr>
              <% unless c.post.menu_id == nil or c.post.menu_id == '' %>
                  <% if c.title == "1" %>
                    <td><%=c.id%></td>
                    <td><%=c.created_at%></td>
                    <td><%=c.writer_email%></td>
                    <td><%=c.post.menu.menu_name%></td>
                    <td><%=c.post.menu.price%></td>
                    <td><%=c.post.receiver_name%></td>
                    <td><%=c.post.receiver_number%></td>
                    <td><%=c.user.email%></td>
                    <td><%=c.tel_number%></td>
                    <td>
                    </td>
                  <% end %>
              <% end %>
              </tr>
            <% end %>
        </tbody>
      </table>
    </div>
    <div id="test3" class="col s12">
      <table class="responsive-table highlight">
        <thead>무통장입금정보
          <tr>
              <th>No.</th>
              <th>신청날짜</th>
              <th>신청자(입금자)</th>
              <th>상품명</th>
              <th>금액</th>
              <th>받는사람</th>
              <th>받는사람번호</th>
              <th>아이디</th>
              <th>핸폰번호</th>
              <th>확인</th>
          </tr>
        </thead>
        <tbody>
            <% @contact.each do |c| %>
              <tr>
              <% unless c.post.menu_id == nil or c.post.menu_id == '' %>
                  <% if c.title == nil && c.created_at < Date.today-2  %>
                    <td><%=c.id%></td>
                    <td><%=c.created_at%></td>
                    <td><%=c.writer_email%></td>
                    <td><%=c.post.menu.menu_name%></td>
                    <td><%=c.post.menu.price%></td>
                    <td><%=c.post.receiver_name%></td>
                    <td><%=c.post.receiver_number%></td>
                    <td><%=c.user.email%></td>
                    <td><%=c.tel_number%></td>
                    <td>
                        
                    </td>
                  <% end %>
              <% end %>
              </tr>
            <% end %>
        </tbody>
      </table>
    </div>
    <div id="test4" class="col s12">
      <table class="responsive-table highlight">
        <thead>회원정보
          <tr>
              <th>No.</th>
              <th>이메일</th>
              <th>이름</th>
              <th>회원가입일</th>
          </tr>
        </thead>
        <tbody>
            <% @users.each do |u| %>
              <tr>
                <td><%=u.id%></td>
                <td><%=u.email%></td>
                <td><%=u.username%></td>
                <td><%=u.created_at%></td>
              </tr>
            <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
<script>
  $(document).ready(function(){
    $('ul.tabs').tabs();
  });
</script>